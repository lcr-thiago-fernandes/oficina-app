# Bounded Contexts e Context Map

## Visão geral dos contextos

```mermaid
graph LR
    subgraph SCS[Sistema da Oficina]
        direction LR

        subgraph CLI[Gestão de Clientes — Suporte]
            Cliente
            Veiculo
        end

        subgraph CAT[Catálogo de Serviços — Suporte]
            Servico
        end

        subgraph EST[Estoque — Suporte]
            Peca
            Movimentacao
        end

        subgraph OS[Ordem de Serviço — NÚCLEO]
            OrdemDeServico
            ItemServico
            ItemPeca
        end
    end

    OS -- "U/S + Conformist<br/>lê cliente/veículo" --> CLI
    OS -- "U/S + Conformist<br/>lê + snapshot" --> CAT
    OS -- "U/S + Conformist<br/>lê + snapshot + baixa" --> EST

    style OS fill:#fef3c7,stroke:#f59e0b,stroke-width:3px
    style CLI fill:#dbeafe,stroke:#3b82f6
    style CAT fill:#dbeafe,stroke:#3b82f6
    style EST fill:#dbeafe,stroke:#3b82f6
```

Legenda: **U/S** = Upstream/Downstream (Customer/Supplier). **Conformist** = downstream aceita o modelo do upstream sem traduzir.

## Classificação de subdomínios

| Contexto | Tipo de subdomínio | Por quê |
|---|---|---|
| **Ordem de Serviço** | **Core (núcleo)** | É o coração competitivo da oficina — onde mora a complexidade (máquina de estados, snapshots, cross-context). Investimento maior em modelagem e testes. |
| **Gestão de Clientes** | Suporte | Necessário, mas não é diferencial competitivo. CRUD com validações de domínio. |
| **Catálogo de Serviços** | Suporte | Necessário para precificar. CRUD simples. |
| **Estoque** | Suporte | Necessário para controle, mas regra principal (saldo ≥ 0) é genérica. Em outra oficina poderia virar Core; aqui é suporte. |
| **Autenticação (módulo técnico)** | Genérico | Identidade/login — qualquer aplicação tem; não é parte do domínio da oficina. |

```mermaid
graph TD
    subgraph DOM[Domínio: Oficina Mecânica]
        Core[Ordem de Serviço<br/>★ NÚCLEO]
        Sup1[Gestão de Clientes]
        Sup2[Catálogo de Serviços]
        Sup3[Estoque]
    end
    subgraph GEN[Genérico — fora do domínio]
        Auth[Autenticação/Identidade]
    end

    Core -.- Sup1
    Core -.- Sup2
    Core -.- Sup3

    style Core fill:#fef3c7,stroke:#f59e0b,stroke-width:3px
    style Sup1 fill:#dbeafe,stroke:#3b82f6
    style Sup2 fill:#dbeafe,stroke:#3b82f6
    style Sup3 fill:#dbeafe,stroke:#3b82f6
    style Auth fill:#e5e7eb,stroke:#6b7280
```

